/** @type {import('next').NextConfig} */
// import { i18n } from './src/libs/i18n'


const nextConfig = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'de'],
  },
};

export default nextConfig;
